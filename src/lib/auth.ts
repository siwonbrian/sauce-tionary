// 실제 로그인 기능(Supabase Auth) 연동 전까지는 항상 비로그인 상태로 고정해둡니다.
// 로그인 여부에 따라 화면을 다르게 보여줘야 하는 곳(BottomNav의 저장됨 탭, 레시피
// 상세 페이지의 내 노트 등)에서 이 값 하나만 공유해서 씁니다.
// 연동 시 이 값을 실제 세션 여부(useSession 훅 등)로 바꾸면 전체 화면에 한 번에 반영됩니다.
export const loggedIn = false;
